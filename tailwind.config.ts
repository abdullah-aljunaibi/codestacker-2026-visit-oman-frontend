import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        primary: "#0A4D5C",
        accent: "#D4A574",
        background: "#F5F0EB",
        text: "#1A1A2E"
      }
    }
  }
};

export default config;
